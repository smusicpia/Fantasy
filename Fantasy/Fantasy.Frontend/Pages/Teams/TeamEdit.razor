@if (teamDTO is null)
{
    <Loading />
}
else
{
    <MudDialog>
        <DialogContent>
            <TeamForm @ref="teamForm" TeamDTO="teamDTO" selectedCountry="selectedCountry" OnValidSubmit="EditAsync" ReturnAction="Return" />
        </DialogContent>
    </MudDialog>
}